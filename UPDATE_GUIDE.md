# 매일 업데이트 작업 지침 (Claude용)

매일 한 번 부동산 세법 변동을 확인하고, 바뀐 게 있으면 `main` 브랜치에 바로 커밋한다.
사람의 승인 단계가 없으므로 **정확성이 최우선**이다. 틀린 숫자를 넣느니 안 넣는다.

## 1. 확인할 것 (공식 출처 우선)
- 법제처(law.go.kr): 소득세법·종합부동산세법·지방세법·지방세특례제한법·공인중개사법 시행규칙 및 각 시행령 최근 개정·시행 이력
- 기획재정부 보도자료 (세제개편안, 시행령 개정), 국세청 공지
- 행정안전부 (취득세·재산세, 공정시장가액비율)
- 국토교통부 (조정대상지역 지정·해제)
- 국회 본회의 세법 통과 뉴스 (보통 12월), 1월 1일·시행일 도래 항목
- 검색 범위는 최근 며칠 위주, 단 `data/updates.json`의 "예정"/"정부안" 항목은 매번 진행 상황 확인

## 2. 반영 규칙
| 상황 | 할 일 |
|---|---|
| 새 발표·개정안 (아직 시행 전) | `updates.json`에만 추가. status `"정부안 · 국회 심의 중"` 또는 `"예정"` |
| 국회 통과·공포됨 | updates.json 해당 항목 status `"확정 · YYYY-MM-DD 시행"`으로 변경 |
| 시행일이 오늘 이전으로 지남 | `tax-rules.json` 수치 반영 + status `"시행 중"` + guide.json 관련 문장 수정 |
| 변경 없음 | `updates.json`의 `lastChecked`만 오늘 날짜로 |

**tax-rules.json 수치 변경 조건 (모두 충족해야 함):**
1. 법령 원문(law.go.kr) 또는 정부 공식 발표문에서 숫자와 시행일을 직접 확인
2. 서로 다른 출처 2곳 이상에서 같은 숫자 확인
3. 시행일이 오늘 또는 그 이전
4. 조건 하나라도 애매하면 수치는 건드리지 말고 updates.json에 `"확인 필요"` 상태로만 적기

## 3. 커밋 전 필수 검사
```
node tests/validate.js
node tests/calc.test.js
```
- validate.js 실패 → 커밋하지 말고 원인 수정. 수정 못 하면 이번 회차 커밋 포기
- calc.test.js 실패가 **법 개정 때문에 기대값이 바뀐 것**이면: 새 기대값을 손으로 다시 계산해서 확인한 뒤 테스트 기대값 수정. 그 외 실패 → 커밋 포기
- tax-rules.json을 바꿨으면 `meta.version`(YYYY.MM.DD), `meta.basis` 갱신

## 4. 커밋
- 메시지: `[YYYY-MM-DD] 세법 업데이트: <요약>` 또는 `[YYYY-MM-DD] 확인 완료 — 변경 없음`
- 수치 변경이 있었으면 커밋 메시지 본문에 항목 / 이전 / 이후 / 시행일 / 출처 URL 적기
- `main`에 바로 push (GitHub Pages가 1~2분 안에 자동 배포)

## 5. 알림
- 계산기 수치(tax-rules.json)를 바꾼 날만 사용자에게 무엇이 바뀌었는지 짧게 알린다 (되돌릴 수 있도록 커밋 링크 포함)
- 변경사항 탭에만 추가했거나 변경이 없으면 조용히 끝낸다
